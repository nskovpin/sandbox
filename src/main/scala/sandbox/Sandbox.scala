package sandbox

object Sandbox {

  def main(args: Array[String]): Unit = {


//    val endTimes = (endMillis :: List.tabulate(chunkCount-1)(n => startMillis + duration * (n+1))).sortBy(v => v)
//    val startTimes = List.tabulate(chunkCount)(n => startMillis + duration * n)
//    (startTimes zip endTimes)



    //.stripMargin

    //  final
    //  def retry[T](n: Int =  3, sleep: Long = 5000)(fn: => T): T = {
    //    Try { fn } match {
    //      case Success(x) => x
    //      case _ if n > 1 =>
    //        Thread.sleep(sleep)
    //        retry(n - 1)(fn)
    //      case Failure(e) =>
    //        throw e
    //    }
    //  }
  }

}
